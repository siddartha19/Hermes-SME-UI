# Cloudflare Pages config for the Alera landing (static selling page + README docs).
# Deploy:  cd alera/landing && npx wrangler pages deploy .
name = "hermes-sme-ui"
pages_build_output_dir = "."
compatibility_date = "2026-07-12"
